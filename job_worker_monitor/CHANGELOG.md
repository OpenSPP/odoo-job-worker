# Changelog — job_worker_monitor

Version history for the `job_worker_monitor` module. Entries are keyed by the
module version in `__manifest__.py`, with the corresponding repository release
tag noted where one exists.

## 19.0.1.0.0 — released as [2026.07](https://github.com/OpenSPP/odoo-job-worker/releases/tag/2026.07) (2026-07-23)

Initial public release: monitoring dashboard, queue metrics with duration
buckets, and configurable alert rules for the `job_worker` engine.
